name: pet-catalog-newrelic
framework:
  type: java_ee
mem: 512M
services:
    catalog: mysql
hooks:
    pre-running:
    - mv newrelic $STACKATO_APP_ROOT/tomcat/
    - cd $STACKATO_APP_ROOT/tomcat/newrelic
    - java -jar newrelic.jar install
